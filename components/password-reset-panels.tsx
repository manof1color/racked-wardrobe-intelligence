"use client";
import Link from "next/link";
import { useState } from "react";

export function ForgotPasswordPanel(){const[email,setEmail]=useState(""),[busy,setBusy]=useState(false),[message,setMessage]=useState(""),[error,setError]=useState("");async function submit(){setBusy(true);setError("");try{const response=await fetch("/api/auth/password-reset/request",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email})});const data=await response.json();if(!response.ok)throw new Error(data.error??"Request unavailable.");setMessage(data.message);}catch(reason){setError(reason instanceof Error?reason.message:"Request unavailable.");}finally{setBusy(false);}}return <AuthResetShell title="Reset your password." copy="Enter your account email. For privacy, Racked shows the same response whether or not an account exists."><div className="auth-fields"><label><span>Email</span><input type="email" autoComplete="email" value={email} onChange={event=>setEmail(event.target.value)}/></label></div>{error&&<div className="form-error" role="alert">{error}</div>}{message&&<div className="success-banner" role="status">{message}</div>}<button className="button button-accent button-full" disabled={busy||!email} onClick={submit}>{busy?"Requesting…":"Send reset link"}</button></AuthResetShell>}
export function NewPasswordPanel({token}:{token:string}){const[password,setPassword]=useState(""),[confirm,setConfirm]=useState(""),[busy,setBusy]=useState(false),[message,setMessage]=useState(""),[error,setError]=useState("");async function submit(){setError("");if(password!==confirm){setError("Passwords do not match.");return;}setBusy(true);try{const response=await fetch("/api/auth/password-reset/confirm",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({token,password})});const data=await response.json();if(!response.ok)throw new Error(data.error??"Password could not be reset.");setMessage(data.message);}catch(reason){setError(reason instanceof Error?reason.message:"Password could not be reset.");}finally{setBusy(false);}}return <AuthResetShell title="Choose a new password." copy="Reset links expire after 30 minutes and work only once."><div className="auth-fields"><label><span>New password</span><input type="password" autoComplete="new-password" value={password} onChange={event=>setPassword(event.target.value)} placeholder="12+ characters"/></label><label><span>Confirm password</span><input type="password" autoComplete="new-password" value={confirm} onChange={event=>setConfirm(event.target.value)}/></label></div>{error&&<div className="form-error" role="alert">{error}</div>}{message&&<div className="success-banner" role="status">{message}</div>}<button className="button button-accent button-full" disabled={busy||!token||!password||Boolean(message)} onClick={submit}>{busy?"Updating…":"Update password"}</button></AuthResetShell>}
function AuthResetShell({title,copy,children}:{title:string;copy:string;children:React.ReactNode}){return <main className="auth-shell"><Link className="wordmark auth-logo" href="/">RACKED<span>.</span></Link><section className="auth-card"><div className="eyebrow">SECURE ACCOUNT RECOVERY</div><h1>{title}</h1><p>{copy}</p>{children}<Link className="settings-return" href="/login">← Return to sign in</Link></section></main>}
